# ============================================================
# Weekly Munger Scanner — Environment variables (TEMPLATE)
# Copy this file to ".env" and fill in real values.
# NEVER commit the real .env file.
# ============================================================

# --- LLM / AI ---
ANTHROPIC_API_KEY=

# --- Web scraping / data sources (fill as tools require) ---
# FIRECRAWL_API_KEY=
# SERPAPI_API_KEY=

# --- Google services (Sheets / Slides deliverables) ---
# Uses OAuth via credentials.json + token.json (see README)
# GOOGLE_APPLICATION_CREDENTIALS=credentials.json
